apiVersion: v1
kind: Service
metadata:
  name: <REPO_NAME>-ui-service
spec:
  type: ClusterIP
  selector:
    app: <REPO_NAME>-ui
  ports:
    - port: 80
      targetPort: 3000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: <REPO_NAME>-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: <REPO_NAME>-ui
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  template:
    metadata:
      labels:
        app: <REPO_NAME>-ui
    spec:
      containers:
        - name: <REPO_NAME>-ui
          image: <IMAGE_NAME>
          imagePullPolicy: Never
          ports:
            - containerPort: 3000
      terminationGracePeriodSeconds: 30
